-- Be aspect ratio correct. Probably black bars with glViewport or setting the destination rect in SDL_RenderCopyEx
   When in regular fullscreen mode, some monitors will put up black bars on their own, so maybe just limit it to windowed
   and borderless modes.
-- Resizable window.
-- Video options menu in glquake.
